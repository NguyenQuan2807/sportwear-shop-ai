spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sportwear_shop?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: abc123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: never

jwt:
  secret: mySuperSecretKeyForJwtToken12345678901234567890
  access-expiration: 3600000
  refresh-expiration: 604800000

app:
  upload:
    dir: ${APP_UPLOAD_DIR:D:/NguyenHuuQuan_DATN/sportwear-shop-ai/backend/uploads}